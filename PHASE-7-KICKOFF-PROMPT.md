# PHASE 7 KICKOFF PROMPT (paste into a new chat)

Start Phase 7, Billing & tiers, for Be Care Compliant (BCC).

CONTEXT (project instructions + memory carry over, but read them and PHASES.md first):
BCC is the live multi-tenant CQC/CIW compliance SaaS. Stack: Next.js 15 App Router + TypeScript + Supabase (Postgres/RLS/Storage/Realtime/Auth) + Tailwind v4 on Vercel; Resend (email), Twilio (SMS, built but no account yet), Anthropic (AI), Stripe (THIS PHASE). Supabase project ref = bgrtcvyjuwopunpnudeu ("Be Care Compliant", eu-west-2). VERIFY this is the target before EVERY migration; never touch joincarenow or carer-academy. Phases 0 to 6 are COMPLETE (Phase 6 signed off bar the first live digest cron, verified separately; do not touch its features). The next migration number is 0056 (0043 to 0055 are Phase 6 notifications, meetings, RSVP, offices and view fixes).

BEFORE WRITING CODE, read: PHASES.md (Phase 7 section, the Additions list, and the Final Testing backlog so you do not duplicate or break logged work), the memory files, and these existing patterns to REUSE (do not rebuild): lib/billing/seats.ts (getSeatUsage / computeSeatUsage / formatPence, already powering the Seats card on /settings and the founder console), companies.tier (business/pro/enterprise/diamond/black, set by the Founder at company creation), usage_events + usage_monthly (per-company SMS and AI metering, built in Phase 6 exactly so Diamond can bill on it), notification_log's claim-then-settle idempotency pattern (copy it for webhook event dedupe), lib/audit.ts writeAudit, middleware PUBLIC_PATHS (webhooks must be added there and verify signatures), user_sessions (0001, single-session enforcement), and the branded email templates in lib/email/templates.ts.

PHASE 7 GOAL (from PHASES.md): Stripe billing and tier enforcement. Fixed and not up for debate: every subscription tier includes 4 users then £5 per extra user per month, with EXACT seat counting (adding a 5th user starts billing, removing them stops it); Diamond has no subscription and pays usage only (SMS + AI from usage_events); Black is free-everything, founder-granted, never purchasable, no Stripe subscription attached. TBC and MUST be popup-confirmed with me before building any billing UI, Stripe products or gating: the CONTENTS and PRICES of Business, Pro and Enterprise. Deliverables: Stripe customer per company, subscription with seat quantity, webhook handling (fail closed, signature verified, idempotent), a Billing page for Company Admins (current tier, seat usage and cost, invoices, payment method), founder billing visibility, server-side tier gating helpers ready for feature flags, and single-session polish (clear "signed out because your account was signed in elsewhere" UX everywhere).

PROCESS: This is a phased project. Do NOT start coding until you have (a) planned the Phase 7 task split and (b) confirmed the load-bearing decisions with me in ONE upfront AskUserQuestion popup, recommended option first labelled "(Recommended)". Maintain the Phases box (master list, Phases 0 to 6 complete) and the Phase Progress box (Phase 7 tasks only) throughout, mirroring PHASES.md. Tier contents/pricing questions may need their own popup: do not invent prices.

DECISIONS TO BRING TO THE POPUP (research current Stripe facts first, cite docs, never training data): Stripe Billing architecture (recommend: one Customer per company, one subscription per subscription-tier company with a licensed base price + per-seat price using quantity = extra seats; Diamond via metered/invoice items from usage_monthly; Black no Stripe objects), checkout approach (recommend Stripe Checkout + customer portal over custom card forms, we never touch card data), webhook endpoint (/api/webhooks/stripe added to PUBLIC_PATHS, STRIPE_WEBHOOK_SECRET fails closed in production, events deduped by event id like notification_log), proration behaviour on seat changes, GBP only, no trials unless I say so, and where the Billing page lives (recommend /settings/billing extending the existing Seats card).

HARD STANDING RULES, THE ONES THAT GOT BROKEN BEFORE, DO NOT REPEAT THEM:
- SAVE BUTTONS (corrected THREE times in Phase 6, never again): every save/submit button is SOLID gold (btn-primary, never btn-outline/btn-ghost for the primary action), shows "Saving…" the instant it is pressed (useActionState pending, inputs disabled), shows "Saved" on success and reverts to "Save" when edited again, and surfaces errors next to the button. Server actions return ActionState ({ok}/{error}), NEVER void, and check the update count so an RLS no-op becomes a visible error, never silence. Canonical implementations: components/settings/branch-form.tsx and components/people/edit-person-form.tsx. Any bare <form action={serverAction}> is a defect.
- Dialogs stay OPEN with a visible working state until the action completes, close immediately on success (no lingering disabled state, it reads as an error), and REMOUNT per open (key per open instance) so stale success state never insta-closes them.
- If my popup answer is a question or confusion, STOP testing and explain in plain visible chat (send_user_message if between tool calls); no popup until I am ready. Prefer driving tests yourself in Chrome (I will open it); verify sends/writes in the database, not by assumption.
- Phone numbers are entered as dialled (07700 900123) and normalised server side to E.164.
- No dashes in ANY customer-facing copy (UI, emails, Stripe product names, statement descriptors): commas, colons, full stops.
- Emails: branded CTA buttons only, never plain-text links; no-op silently if RESEND_API_KEY/RESEND_FROM missing and flag the dependency. No "Open Be Care Compliant" button for recipients without app accounts.
- Security DB-enforced: every new table carries company_id with RLS via is_company_member / is_company_admin / is_platform_admin; privileged writes via SECURITY DEFINER RPCs with pinned search_path guarded by record ownership (a user-callable SECURITY DEFINER RPC still runs with the caller's auth.uid()); service-role client never in a client component; secrets never NEXT_PUBLIC_. Stripe secret keys and webhook secrets are server-only env vars.
- Beware SQL null traps in predicates (response = 'x' is NULL for null values, cost us a live bug in Phase 6): coalesce comparisons in views and policies.
- Tier gating enforced SERVER-SIDE (and DB where possible), never UI-only. Billing amounts in pence integers, never floats.
- Never use "item" or "board" anywhere. Terminology: Founder = platform owner; Admin = company role.
- Research present-day Stripe pricing/API behaviour and cite docs. Verify, don't guess: use Supabase and Vercel tools to inspect actual state before theorising. "Done" means traced, not typechecked.

DEPLOY WORKFLOW (critical):
- Migrations through the Supabase MCP tools to ref bgrtcvyjuwopunpnudeu ONLY (verify first), AND write each as a numbered SQL file in supabase/migrations (next = 0056).
- Vercel auto-deploys on push to main; verify deploy state with the Vercel tools, never assume. The Vercel build is the compile gate (sandbox cannot npm/build or reach GitHub).
- I build/push on my machine. End each piece of work with EXACTLY ONE copy-paste terminal block: cd "/Users/phildavies/Library/Mobile Documents/com~apple~CloudDocs/Be Care Compliant/becarecompliant" && git add -A && git commit -m "…" && git push. If git complains about a stale lock, prefix rm -f .git/index.lock. Never prose git steps.
- Stripe setup is NEW: when I must do anything in the Stripe dashboard (account, products, prices, webhook endpoint, signing secret) give me numbered steps with the EXACT values to paste (URLs, names, env var names), one step at a time, and add STRIPE_SECRET_KEY / STRIPE_WEBHOOK_SECRET to Vercel env walkthroughs. Same for any Vercel env change.
- Never ask me to test before confirming the code is deployed AND migrations applied.
- Test as we go via popups (Pass / Fail / Not tested), one check at a time, in TEST-CHECKLIST-PHASE7.md; log anything untested straight into Final Testing. Use Stripe test mode end to end before any live key is discussed.

Start by reading PHASES.md + memory + the reuse points above, then come back with the single Phase 7 planning popup (task split + the decisions above). Keep questions to that one popup, except tier contents/pricing which get their own popup with your researched recommendations (competitor pricing context is in the jcn-research skill if useful).
