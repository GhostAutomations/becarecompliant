Start Phase 4 — the Service Users section — of Be Care Compliant. Phase 3 (People) is complete and signed off; build Phase 4 to the same standard, reusing as much of Phase 3 as possible. Before writing any code, read PHASES.md (Phase 4 section) and PHASE-4-BRIEF.md in the repo — they are the agreed source of truth — and check the codebase for what already exists so we extend the shared helpers rather than build parallel ones. Keep the Phases and Phase Progress boxes updated as we go.

Context you need up front (already agreed with me, do not re-ask):

Reuse from Phase 3 (extend/share, do NOT rebuild): the register matrix (sticky first column, centred cells, dense rows, DD MMM YY dates, RAG cells, inline colour-coded portal pill dropdowns, permanent horizontal scrollbar, search + sort, column shorthand labels in Settings); the Views + Branches dropdowns with the same instant client-side switching (load once, filter on the client, URL kept in sync, "Summary" label); branch auto-fill of manager/supervisors on Add; the record drill-down; the compliance loop (complete Form → immutable Evidence → advance check via the shared recurrence engine); the shared forms engine (renderer, validator, conditional logic, preset answers, activity-date completion); realtime live refresh; toast host; back-to-view links; canonical controls; instant "Saving" button; client-side redirect after Server Actions (never redirect() to a query-string URL — use ActionState.redirectTo + router.replace); on-demand evidence PDF; RLS helpers + SECURITY DEFINER RPCs with pinned search_path.

Service User record: name ("Service User"), ssid (Social Services ID, unique within company), package_start_date, service_status enum (active / hospital / respite / cancelled), branch. Never mixed with People in UI or data model. This is special-category health data — the most sensitive in the platform.

Main view columns, in this order: 1) Service User (sticky, links to record) 2) Package Start Date 3) SSID 4) Status — inline pill (Active / Hospital / Respite / Cancelled) 5) Most Recent Review 6) New Review Due 7) Planned Review Date 8) Review Status — inline pill (Awaiting Review / Booked In / Overdue). Review columns only for now; risk assessment, MAR audit and consent review live in the record drill-down.

Views: Main (Active) + Hospital + Respite + Cancelled + Summary, mirroring People's Main/Leavers/LTS/Archive. Cancelled behaves like Leavers (excluded from the active register, rollups, dashboard and reminders, kept for audit). The Status pill moves a Service User between views instantly, with a "Moved to X" toast, exactly like People.

Review workflow: the primary Service User check is the Care Plan Review (recurring). Most Recent Review = last completed; New Review Due = last review + interval (recurrence engine); Planned Review Date = the booked date for the next review. Review Status is AUTO-DERIVED (no manual bookkeeping): Overdue when New Review Due has passed; Booked In when a Planned Review Date is set and it is not overdue; Awaiting Review otherwise.

GDPR (higher bar than People): access audit logging on READ of a Service User record and its evidence, not just writes; strict role isolation in RLS; Team Members never see Service User data unless explicitly assigned; Supervisors scoped to caseload; cancelled/discharged excluded everywhere.

Follow the project rules throughout: vocabulary Record/Register/Check/Form/Evidence (never item/board); no dashes in customer-facing copy; all decisions and testing as AskUserQuestion popups with my recommended option first; apply migrations to the becarecompliant Supabase project only (verify the ref first) and also write each as a numbered SQL file in supabase/migrations; end each piece of work with a single copy-paste git push block; verify state with the Supabase/Vercel tools rather than guessing.

Start by planning the Phase 4 build as a set of tasks (popup to agree the split with me) before writing code, then create the Phase Progress tasks for it.
