# PHASE 8 KICKOFF PROMPT (paste into a new chat)

Start Phase 8, Reporting, exports and audit trail, for Be Care Compliant (BCC).

CONTEXT (project instructions + memory carry over, but read them and PHASES.md FIRST): BCC is the live multi-tenant CQC/CIW compliance SaaS. Stack: Next.js 15 App Router + TypeScript + Supabase (Postgres/RLS/Storage/Realtime/Auth) + Tailwind v4 on Vercel; Resend (email), Twilio (SMS, built but no account yet), Anthropic (AI), Stripe (billing, live in test mode). Supabase project ref = bgrtcvyjuwopunpnudeu ("Be Care Compliant", eu-west-2). VERIFY this is the target before EVERY migration; never touch joincarenow or carer-academy. Phases 0 to 7 are COMPLETE (Phase 7 billing signed off 2026-07-12; do not touch its features). The next migration number is 0058 (0056 = billing tables, 0057 = realtime users list).

BEFORE WRITING CODE, read: PHASES.md (the Phase 8 section, the Additions list, and the WHOLE Final Testing backlog so you neither duplicate nor break logged work, and so you CLEAR the Phase 8 cold items it already lists), the memory files, and these existing patterns to REUSE (do not rebuild):
- lib/evidence/pdf.tsx (renderEvidencePdf, deterministic @react-pdf/renderer render of one Evidence from its frozen schema_snapshot) and lib/evidence/storage.ts (EVIDENCE_BUCKET, SIGNED_URL_TTL_SECONDS = 300 i.e. 5 minutes, signEvidenceDownload, sha256Hex, evidencePdfPath). Evidence is stored as an IMMUTABLE snapshot (answers + pinned form_version_id + embedded schema_snapshot); the PDF is regenerated on demand, identically, from that snapshot. This phase builds the on-demand render + 5-minute signed-URL download (audit-logged), NOT a re-store.
- lib/audit.ts writeAudit + the audit_log table (columns: company_id, actor_id, actor_email, actor_role, action dotted-verb, entity_type, entity_id, summary, metadata jsonb, ip, created_at). It is APPEND-ONLY: no insert/update/delete policies, written only by the service-role client; read policy = is_company_admin(company_id) OR is_platform_admin(); REPLICA IDENTITY FULL already set. There are ~23 writeAudit call sites today; a big part of THIS phase is widening that coverage (see the audit-trail goal).
- The RAG rollup views and helpers: person_rollup, person_check_status, service_user rollup/status views, check_rag, and the recurrence engine lib/recurrence.ts. Reports READ these; do not recompute RAG in a new place.
- lib/billing/tier.ts (tierHasFeature / featureEnabled / requireFeature). "reporting_exports" is already defined as a Pro-and-above feature: gate exports server-side through it.
- UI primitives: components/back-link.tsx, components/register/pill-select, the horizontal-scrollbar, the glass-card / pill-* / app-tile classes in app/globals.css, components/realtime-refresh.tsx, and the canonical form controls. Mirror an existing screen; do not invent a new visual language.
- Terminology (hard rule): Record, Register, Check, Form, Evidence. NEVER "item" or "board". Founder = platform owner; Admin = company role.

PHASE 8 HAS TWO PILLARS. Confirm scope for BOTH in the planning popup before coding.

PILLAR A, REPORTING AND EXPORTS (from PHASES.md): build ONE shared export module and route everything through it (a single lib/export/* with a PDF helper on @react-pdf/renderer and a CSV helper; never scatter ad-hoc CSV string-building). Deliverables:
- On-demand Evidence PDF: render any completed Evidence from its frozen snapshot to a branded PDF, download via a 5-minute signed URL from the private bucket, and WRITE an evidence.downloaded audit row on every download. (Clears the parked "evidence PDF generated on demand" + "5-minute signed-URL download audit-logged" Final Testing items.)
- Inspection-ready Evidence pack per Record: all Evidence for a Person or Service User (with author, timestamp, form version) as one PDF a manager can hand to a CQC/CIW inspector or LA contract officer, plus a CSV index.
- Register and branch compliance reports: RAG summary and overdue lists per Register and per Branch, rolling up to the company dashboard, exportable PDF + CSV. Leavers/archived/discharged excluded from active reports (respect the same exclusions as the registers).
- Format must be inspector-facing and calm: clear RAG, dates, who did what and when. No dashes in any copy.

PILLAR B, AUDIT TRAIL (Phil's explicit ask: "when changes are made, they are traceable"). This is a BUILD, not just a viewer:
- Coverage: make every meaningful change across the app write an audit_log row through writeAudit, capturing who / what / when and, where it matters, old -> new values in metadata. Audit the mutation sites that are currently silent: People and Service User create/edit/transfer/archive/status changes, check-definition and check-config changes, form publish/draft lifecycle, holiday and absence decisions and meeting actions, user and role and branch changes, billing changes, and record deletes. (Check completion already lands as immutable Evidence; reference it rather than duplicating.)
- GDPR read-audit (special-category data): opening a Service User record writes a service_user.viewed audit row; Evidence view/download writes an audit row. (Clears the parked "GDPR read audit" and "evidence view/download audit" Final Testing items.) Service User data is special-category under UK GDPR, so treat access-logging as first-class.
- Audit trail VIEWS: a per-Record history timeline (drill-down tab showing that record's changes + Evidence in one chronological, oldest-at-top newest-at-bottom order), a per-company Audit log viewer for Company Admins (filter by actor, entity, date; read-only), and a Founder cross-company audit console. All read-only, DB-enforced (audit_log read policy already scopes Admin to own company, Founder to all). Exportable via Pillar A (PDF + CSV) so an inspector can be handed the trail.

PROCESS: phased project. Do NOT start coding until you have (a) planned the Phase 8 task split into the Phase Progress box and (b) confirmed the load-bearing decisions with me in ONE upfront AskUserQuestion popup, recommended option first labelled "(Recommended)". Maintain the Phases box (0 to 7 complete) and the Phase Progress box (Phase 8 tasks only) throughout, mirroring PHASES.md. Research present-day CQC (single assessment framework) and CIW (inspection framework) and any LA contract-monitoring expectations for what an inspector-facing evidence pack / audit trail should contain, and CITE sources; do not quote training data.

DECISIONS TO BRING TO THE PLANNING POPUP (put your researched recommendation first):
- Report set and where it lives: recommend a new /reports area (register report, branch/company compliance report, per-record evidence pack, audit-trail report) PLUS contextual "Export" buttons on the record drill-downs and registers, all routed through the shared export module. Confirm the exact report list.
- PDF engine: recommend reusing @react-pdf/renderer (already proven for Evidence) for all report PDFs, and one small CSV helper; no new dependency. Confirm.
- Exports tier gating: recommend reporting_exports = Pro and above (server-side via requireFeature), with a clear upgrade state on Business. Confirm (is any export available on Business, e.g. a single record's own evidence?).
- Audit-trail depth: confirm the exact list of entities/actions to audit and where old -> new value capture is required (e.g. status changes, transfers, role changes, check-config edits), plus retention (audit_log currently unbounded) and whether IP is captured.
- GDPR read-audit scope: confirm service_user.viewed on open and Evidence view/download are the read-events to log (and whether People record opens are logged too).
- Probation extension in reports (PARKED OPEN QUESTION from Phase 3, must be answered before building the report format): how an extended probation is represented in compliance reports and inspector exports (original end due, actual end, extension date, status). Bring options.
- Signed-URL TTL and immutability: reuse 5 minutes; confirm.

FIX THESE QUALITY DEFECTS AS PART OF THIS PHASE (Phil: "iron out any little mistakes like save buttons"):
- SAVE BUTTON SWEEP (logged in Final Testing): convert EVERY remaining bare `<form action={serverAction}>` with a void action to the canonical useActionState pattern (solid btn-primary, instant "Saving…", inputs disabled, "Saved" on success reverting to "Save" when edited, errors shown next to the button, update-count checked so an RLS no-op surfaces as a visible error, action returns ActionState never void). Known offenders: app/(app)/founder/page.tsx (setCompanyStatus x3), app/(app)/settings/users/page.tsx (resend/revoke invite), app/(app)/settings/actions.ts setUserStatus + deleteUser (added in Phase 7 as void), app/(app)/people/[id]/page.tsx (applyMissingChecks, updateTracker x2, transferPerson, assignSupervisor, setEmploymentStatus, setArchived), app/(app)/service-users/[id]/page.tsx (applyMissingChecks, transferServiceUser, assignServiceUserSupervisor, setServiceStatus, setArchived). Canonical implementations: components/settings/branch-form.tsx, components/people/edit-person-form.tsx. (Many of these mutation sites are exactly where you are adding audit coverage in Pillar B, so do both together.)
- DELETE-USER DIALOG: components/settings/team-member-controls.tsx uses a native window.confirm() for "Delete user". Replace it with the app's standard slide-over/dialog pattern (remounts per open, stays open with a working state, closes on success) per the dialogs standing rule, for consistency and so it is testable.

HARD STANDING RULES (do not repeat past mistakes):
- Save buttons and dialogs exactly as above. Any bare `<form action={serverAction}>` or a `"use server"` file exporting a non-async value is a defect. "use server" files export only async functions; keep consts/objects in lib/.
- Server actions return ActionState ({ok}/{error}/{redirectTo}); NEVER redirect() from a Server Action to a URL with a query string (React #310 / Next #78396) — do client router.replace, or window.location for external URLs.
- If my popup answer is a question or confusion, STOP and explain in plain visible chat (send_user_message between tool calls); no popup until I am ready. Prefer driving tests yourself in Chrome (I will open it and sign in); verify reads/writes in the database, not by assumption. Native confirm() dialogs block browser automation, so a human click may be needed.
- No dashes in ANY customer-facing copy (UI, emails, reports, PDF, CSV headers): commas, colons, full stops.
- Security is DB-enforced: every new table carries company_id (+ branch_id where relevant) with RLS via is_company_member / is_company_admin / is_platform_admin; privileged writes via SECURITY DEFINER RPCs with pinned search_path guarded by record ownership (a user-callable SECURITY DEFINER RPC still runs with the caller's auth.uid()); the service-role client never appears in a client component; secrets never NEXT_PUBLIC_. Private Storage buckets with short-lived signed URLs for every file; downloads audit-logged. audit_log stays append-only (service-role write only).
- Beware SQL null traps in predicates (response = 'x' is NULL for null; coalesce comparisons in views and policies). Realtime gotcha: RLS tables need REPLICA IDENTITY FULL and publication membership; subscribe unfiltered + poll fallback.
- Tier gating enforced SERVER-SIDE (and DB where possible), never UI-only. Dates/RAG use the shared recurrence engine and Europe/London. Money in pence integers.
- GDPR: each customer company is controller, BCC is processor; Service User data is special-category, so audit access not just change, signed URLs for files, and design retention/deletion in. Flag GDPR impact on any feature touching personal data. SAR export/anonymisation already have groundwork (sar_evidence_for_subject, anonymise_evidence) — reuse, do not rebuild.
- Verify, don't guess: use the Supabase and Vercel tools to inspect real state before theorising. "Done" means traced, not typechecked: trace every mutation site you add audit to, and every export path, before declaring complete.

DEPLOY WORKFLOW (critical):
- Migrations through the Supabase MCP tools to ref bgrtcvyjuwopunpnudeu ONLY (verify the project before every apply), AND write each as a numbered SQL file in supabase/migrations (next = 0058).
- Vercel auto-deploys on push to main; verify deploy state and build logs with the Vercel tools, never assume. The Vercel build is the compile gate (the sandbox cannot npm/build or reach GitHub).
- I build/push on my machine. End each piece of work with EXACTLY ONE copy-paste terminal block: cd "/Users/phildavies/Library/Mobile Documents/com~apple~CloudDocs/Be Care Compliant/becarecompliant" && git add -A && git commit -m "…" && git push. If git complains about a stale lock, prefix rm -f .git/index.lock. Never prose git steps, never multiple blocks. If a new npm dependency is added, include npm install in that one block before the commit.
- Anything I must do in an external app (Supabase dashboard, Vercel env, DNS) gets a numbered step-by-step walkthrough with the exact values to paste.
- Never ask me to test before confirming the code is deployed AND its migrations applied. Test as we go via popups (Pass / Fail / Not tested), one check at a time, into TEST-CHECKLIST-PHASE8.md; log anything untested straight into Final Testing.

START by reading PHASES.md (Phase 8 section + Additions + the full Final Testing backlog) and the memory files, research the CQC/CIW inspector-evidence and audit expectations (cite), then come back with the single Phase 8 planning popup: the task split plus the load-bearing decisions above (report set + location, PDF/CSV engine, exports tier gating, audit-trail depth and old->new capture, GDPR read-audit scope, probation-extension representation, signed-URL TTL). Then build, testing as you go.
